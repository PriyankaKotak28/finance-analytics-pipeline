# finance-analytics-pipeline
built an end-to-end data pipeline that ingests 10,000+ banking transactions, cleans and transforms the data using Python and pandas, stores it in SQL, and visualises spending trends in Power BI — mimicking a real bank's internal analytics workflow.
